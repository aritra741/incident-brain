# Backend origin (no trailing slash). Required for Vercel when the API is on another host (e.g. Railway).
# Local dev: leave unset — Vite proxies /api and /ws to localhost:8000.
# VITE_API_BASE_URL=https://your-service.up.railway.app
